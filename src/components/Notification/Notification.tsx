import React from 'react'

interface NotificationProps{
    content:String;
}

const Notification = (props:NotificationProps)=>{
    return <div>Hello this is a notification</div>
}

export default Notification;